module.exports = {
    theme: {
        extend: {
          fontFamily: {
            line: ['LINE Seed']
          }
        }
    },
    plugins: [
      require('@tailwindcss/typography'),
    ]
  }